
 * FSharpLint Documentation — Main guide for FSharpLint, a static analysis linter for F#.
 * FSharpLint Rule FL0065 — Specific rule reference page detailing code refactoring suggestions.
 * Mascot — A style-checker and code metrics tool for OCaml source code.
 * Fantomas — The standard source code formatter for F#.
 * OCamlformat — Auto-formatter for OCaml code, part of the official OCaml Platform.
 * Zanuda — A linter for OCaml and dune-based projects designed to catch code smells and bugs.
 * Typerex-lint (ocp-lint) — An extensible style checker and linter for OCaml source files.
 * F# Formatting — A collection of F# tools for markdown processing and code formatting used in documentation generation.

 * FSharp.Analyzers.SDK — A framework for writing custom real-time static analyzers and code fixes for F# editors.
 * G-Research FSharp Analyzers — A production-tested collection of F# code quality analyzers built on top of the Ionide SDK.
 * FsAutoComplete — The core F# language server (LSP) that surfaces FSharpLint diagnostics and analyzer warnings inline within IDEs.
 * FSharp.LintKit — A lightweight framework designed for creating custom, project-specific lint rules and guardrails.
 * F# Compiler Service — The official API for parsing, type-checking, and analyzing F# syntax trees (ASTs), powering most F# linters.
 * Ionide for VS Code — The primary F# editor extension, providing inline linting, Fantomas formatting, and code diagnostics.
 * Exercism F# Analyzer — An AST-based analyzer for evaluating F# submissions and suggesting idiomatic code improvements.
 
 * FSharp.Formatting — Documentation generation and literate programming tool that parses F# code and inline Markdown to generate type-checked documentation.
 * F# Language RFCs (fslang-rfcs) — The official design RFC repository documenting approved syntax changes, compiler warnings, static analysis additions, and diagnostic codes.
 * FSharp.Compiler.Service UntypedAST Guide — Official reference documentation for traversing and inspecting F#'s Untyped Abstract Syntax Tree (ParsedInput) for custom static analysis tools.
 * FSharp.Data Type Providers — Compile-time static type provider library that validates JSON, CSV, XML, and HTML schemas directly during compilation.
 * Stryker.NET Mutation Testing — A mutation analysis framework for .NET that mutates F# AST nodes to verify test suite quality and code resilience.
 * Fable Compiler — An AST-transforming compiler that performs static analysis on F# code to produce idiomatic JavaScript, Python, TypeScript, or Rust.
 * FsCheck — Property-based testing and random specification tool used to verify program invariants and edge-case behavior statically and dynamically.
 * SonarScanner for .NET — Enterprise static security scanner and code analyzer for .NET solutions including F# projects.
